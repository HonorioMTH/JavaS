export class celular{
    marca: string;
    modelo: string;
    polegadas: number;
    cameras: number;

    constructor(
    marca: string,
    modelo: string,
    polegadas: number,
    cameras: number,
    ){
        this.marca = marca
        this.modelo = modelo
        this.polegadas = polegadas
        this.cameras = cameras
    }
}